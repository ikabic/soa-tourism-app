[INPUT]
    Name        forward
    Listen      0.0.0.0
    Port        24224

[OUTPUT]
    Name        loki
    Match       *
    Host        loki
    Port        3100
    labels      job="fluent-bit"
    label_keys  $container_name
    remove_keys source
    line_format json